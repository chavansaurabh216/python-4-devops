# In this we will be calling the functions we created in each practice code and test it

from is_prime import is_prime_number
is_prime_number()

from is_prime_number_interval import prime_range
prime_range()

from is_armstrong import is_armstrong
is_armstrong()

from is_armstrong_interval import is_armstrong_interval
is_armstrong_interval()